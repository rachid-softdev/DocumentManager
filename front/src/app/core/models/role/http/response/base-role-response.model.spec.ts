import { BaseRoleResponse } from './base-role-response.model';

describe('BaseRoleResponse', () => {
  it('should create an instance', () => {
    expect(new BaseRoleResponse()).toBeTruthy();
  });
});
