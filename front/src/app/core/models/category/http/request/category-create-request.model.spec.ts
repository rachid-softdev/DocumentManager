import { CategoryCreateRequest } from './category-create-request.model';

describe('CategoryCreateRequest', () => {
  it('should create an instance', () => {
    expect(new CategoryCreateRequest()).toBeTruthy();
  });
});
