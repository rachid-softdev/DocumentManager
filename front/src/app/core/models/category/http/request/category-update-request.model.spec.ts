import { CategoryUpdateRequest } from './category-update-request.model';

describe('CategoryUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new CategoryUpdateRequest()).toBeTruthy();
  });
});
