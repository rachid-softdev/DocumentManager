import { BaseCategoryResponse } from './base-category-response.model';

describe('BaseCategoryResponse', () => {
  it('should create an instance', () => {
    expect(new BaseCategoryResponse()).toBeTruthy();
  });
});
