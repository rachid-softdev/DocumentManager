import { CategoryResponse } from './category-response.model';

describe('CategoryResponse', () => {
  it('should create an instance', () => {
    expect(new CategoryResponse()).toBeTruthy();
  });
});
