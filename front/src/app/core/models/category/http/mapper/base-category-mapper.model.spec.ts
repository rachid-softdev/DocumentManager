import { BaseCategoryMapper } from './base-category-mapper.model';

describe('BaseCategoryMapper', () => {
  it('should create an instance', () => {
    expect(new BaseCategoryMapper()).toBeTruthy();
  });
});
