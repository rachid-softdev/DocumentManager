import { BaseDocumentResponse } from './base-document-response.model';

describe('BaseDocumentResponse', () => {
  it('should create an instance', () => {
    expect(new BaseDocumentResponse()).toBeTruthy();
  });
});
