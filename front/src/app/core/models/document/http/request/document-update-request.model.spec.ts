import { DocumentUpdateRequest } from './document-update-request.model';

describe('DocumentUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new DocumentUpdateRequest()).toBeTruthy();
  });
});
