import { DocumentCreateRequest } from './document-create-request.model';

describe('DocumentCreateRequest', () => {
  it('should create an instance', () => {
    expect(new DocumentCreateRequest()).toBeTruthy();
  });
});
